FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# In dev, source is mounted — no COPY needed
RUN mkdir -p /app/data/cv_files

EXPOSE 8000
